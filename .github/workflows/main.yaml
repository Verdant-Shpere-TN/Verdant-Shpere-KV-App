name : "main workflow"

on: 
  pull_request:
    branch:
      - dev

jobs:
  build:
    name: "Build laravel App"
    runs-on: ubuntu-latest
    steps:
      - name: Hello
        run: |
          echo "hello"

