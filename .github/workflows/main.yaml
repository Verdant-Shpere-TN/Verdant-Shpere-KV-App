name : "main workflow"

on: 
  pull request:
    branch:
      - dev

jobs:
  build:
    name: "Build laravel App"
    runs-on: ubuntu-latest
    steps:
      - name: Hello
        run: |
          echo "hello"

